{
  "name": "campushub",
  "private": true,
  "scripts": {
    "dev:backend": "cd backend && npm run dev",
    "dev:frontend": "cd frontend && npm run dev",
    "dev:mobile": "cd mobile && npx expo start",
    "install:all": "cd backend && npm install && cd ../frontend && npm install && cd ../mobile && npm install",
    "install:mobile": "cd mobile && npm install",
    "build:frontend": "cd frontend && npm run build",
    "build:apk": "cd mobile && eas build --platform android --profile preview",
    "build:aab": "cd mobile && eas build --platform android --profile production",
    "start:backend": "cd backend && npm start",
    "start:frontend": "cd frontend && npm start"
  }
}
